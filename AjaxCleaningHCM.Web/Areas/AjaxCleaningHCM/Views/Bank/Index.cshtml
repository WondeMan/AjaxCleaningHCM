@model AjaxCleaningHCM.Domain.DTO.MasterData.Response.BankResponseDtos
@{
    Layout = "_Default";
}
<div class="table-responsive">
    <table id="table" class="table table-striped">
        <thead>
            <tr>
                <th>
                    No
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BankDtos.FirstOrDefault().Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BankDtos.FirstOrDefault().Code)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BankDtos.FirstOrDefault().Description)
                </th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            @{
                int index = 0;
            }
            @foreach (var item in Model?.BankDtos?.ToList())
            {
                index++;
                <tr>
                    <td>@index</td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Code)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        <div class="action-icons">
                            <a asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-edit text-info"></i></a> |
                            <a class="btn" onclick="deleteEntity('Bank','@item.Id')"><i class="fas fa-trash text-danger"></i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
